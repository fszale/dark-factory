# Release evidence snapshot

Date: 2026-09-19. This is a local evidence ledger, not a deployment certificate.

## Passed local evidence

- The final source snapshot passed `npm run typecheck` and **107 tests across 12 files**. The suite covers process-wide provider concurrency, command-ID collisions (including imported checkpoints), invalid provider output, large bounded checkpoint import, nonduplicated archive starts, decision aftermath, shared runtime response schemas, monotonic live-stream sequencing, station-specific pause/resume, defect instrumentation, deterministic and adaptive paired experiments, adaptive job isolation, and cancellation on takeover, explicit cancel, and session expiry.
- A fresh production build completed inside Docker from that source snapshot on 2026-09-19. It compiled the SPA and bundled the server, experiment worker, and replay worker. The image build's production and development dependency installs reported zero known vulnerabilities.
- The reproducible harness is [`scripts/release-smoke.mjs`](../scripts/release-smoke.mjs); set `SMOKE_BASE` or `BASE_URL` to the server origin. An isolated production smoke on localhost:3011 used host Node **22.23.2** and the built `dist` artifacts. It passed health and static SPA checks; session creation and cross-session-token rejection; truthful 503 responses for both interactive and adaptive AI when providers are unconfigured; the configured-policy 10-paired-seed experiment worker; rejection of recorded-AI comparison without live applied decisions; commands; graphical replay through the replay worker; export/import; authenticated NDJSON history; and first-message WebSocket authentication with session ID, monotonic sequence, and wall-clock timestamp. The temporary process was stopped after the run.
- Fresh arm64 image `brickworks-dark-factory:release-20260919-final` built successfully from the visually accepted final source with container Node **22.23.1**. Image manifest digest: `sha256:2a27c93f405dc1e932e9f4f4e291a63b16057654e98f79d5f1e79c657354819b`; recorded size: 128,467,905 bytes. Its isolated localhost:3012 container passed the same smoke and was removed after the run. The image's server and worker hashes exactly match [`release-soak-source.json`](review/release-soak-source.json), tying the container's runtime code to the localhost:3010 soak runtime while including the final accepted gripper and export UI changes. The existing localhost:3010 and localhost:3009 runtimes remained healthy and were not restarted.
- Isolated browser acceptance at localhost:5173 passed live controls, metrics, comparison disclosure, fault/repair, mix-control persistence, selected-vehicle genealogy, follow, and replay. See [browser controls](browser-controls.md).
- Simulation, archive, replay, road reservation, conservation, and bounded-retention evidence is detailed in [verification](verification.md).
- Visual evidence is indexed in [guided tour](guided-tour.md) and [graphics review](graphics-review.md). The controlled final 1080p stress run rendered 3,184 meshes for 72 seconds on Apple M5 and averaged **107.6 FPS** across 72 samples, with an **86.9 FPS** minimum and **102.6 FPS** fifth percentile; see [`final-stress-1080p.png`](review/final-stress-1080p.png). An earlier 11.9 FPS sampled minimum occurred during concurrent builds and background load and is retained as an uncontrolled run, not used as acceptance evidence.
- Authorized live-provider checks returned HTTP 200 for Astra advisory (4,699 ms, 1,194 reported tokens) and Jev advisory (515 ms, 1,513 reported tokens). Astra autonomously applied a validated front-station repair; a second isolated run retained a complete 120-simulated-second observational aftermath. A browser Astra response requested a bounded ten-seed `front:gentle` experiment without changing the live profile. Jev completed a live adaptive ten-paired-seed job with twenty hold decisions under the twelve-per-minute limit; all sixteen candidate metrics matched baseline exactly. Evidence: [`live-provider-check.json`](review/live-provider-check.json), [`live-astra-autonomous.json`](review/live-astra-autonomous.json), [`live-astra-aftermath.json`](review/live-astra-aftermath.json), [`live-astra-browser.txt`](review/live-astra-browser.txt), and [`live-adaptive-jev.json`](review/live-adaptive-jev.json).
- The public Replit deployment returned HTTP 200 for the application and `/api/health` on 2026-09-20. A nonpaid disposable production smoke created two isolated sessions, rejected a cross-session token with 401, rejected unauthenticated Astra and Jev requests with 403 before reaching either provider, and authenticated a live WebSocket whose sequence advanced from 14 to 77 across 64 observed frames. At 92.5 simulated seconds the run had passed one receiving inspection, completed one vehicle, entered the parking path, and retained zero material and receiving conservation deltas. Evidence: [`production-smoke.json`](review/production-smoke.json).
- In the release thread, the signed-in Replit deployment view showed Live status on a 0.5 vCPU / 2 GiB Reserved VM. A public in-app-browser run reached 1:40 simulated time with `taxi-1` parked and restored its paused checkpoint after the already-authorized republish. These are dated operator observations, distinct from the reproducible REST/WebSocket artifact above; no secret values were inspected or recorded.

## Evidence still pending or unavailable

- Authenticated Astra and Jev production advisory responses passed on 2026-09-20; see the sanitized evidence below. Provider quality, autonomous improvement and long-run stability remain unproven.
- The corrected two-hour server/WebSocket soak **passed** against localhost:3010: [`release-soak-corrected.json`](review/release-soak-corrected.json) contains 121 scheduled samples through 7200.012 wall-clock seconds, 1,589 completed and 1,586 dispatched vehicles, strict frame growth from 1 to 56,809, zero material/receiving deltas and stream errors, and retained event/sample maxima of 2,000/720. [Its assessment](review/release-soak-corrected-assessment.md) records sampled heap/RSS ranges, the exact runtime identity, and five independently rechecked source hashes. It made no AI calls or credential use.
- [`release-soak.json`](review/release-soak.json) remains preserved as the earlier failed finalization artifact, not evidence of a stopped live stream; see its [independent assessment](review/release-soak-assessment.md). The pre-station-pause partial run is also preserved separately.
- Live checks are bounded local evidence, not a general evaluation of provider quality, long-run reliability, model stability, cost, or safety. Jev's adaptive policy chose hold twenty times and produced no improvement or regression; no applied Jev autonomous action was forced. Astra's 120-second aftermath is labeled observational, not causal.
- Export content and API delivery passed, and browser instrumentation observed an 18,041-byte download begin event. The in-app browser cancelled the file transfer and does not support the required download-behavior override, so saving that file through this browser surface remains unverified.
- No formal browser audio listening or full accessibility evaluation is recorded. Final dusk, exploded, and 1080p stress views have been reviewed; see [graphics review](graphics-review.md).
- The project is a simulation. No evidence covers physical mechanisms, forces, tolerances, collision safety, factory safety systems, or commissioning.

## Release decision guidance

A technical release can cite the completed local build, Node 22 smoke, Docker smoke, browser-control evidence, bounded local live-provider evidence, the passed two-hour server/WebSocket soak, and the bounded public Replit checks above. Do not represent it as production-provider-certified, audio-certified, browser-file-save-verified, accessibility-certified, or physically validated until the pending evidence above exists.

The final receiving flow includes visible five-way pallet sorting and fourteen authoritative simulated seconds before warehouse acceptance. The final source snapshot suite reports 107 tests across 12 files. Browser reload reuse and real server-restart recovery are documented in `docs/browser-controls.md`.

## Additional replay failure verification — 17:11 EDT

The full suite passed 107 tests across 12 files and typecheck after adding an API regression test for invalid replay input, unavailable future history, unchanged source state, and released replay capacity following errors. Runtime source and built artifacts were unchanged, so the active endurance run remains valid. Quota-truncated replay rejection is covered by the existing server test. These are API-level assertions; browser presentation of these error cases and rework history remains unobserved.


## Graphics update published — 2026-09-20

Replit republish completed with destination status **Live** on the existing 0.5 vCPU / 2 GiB VM. Before publication, its clean checkout was `35706e1`, including graphics commit `f3b9392`; Replit's publication commits retain the VM port configuration. Public health returned HTTP 200 after the server restarted, both providers remained configured, and access-code enforcement remained enabled. The public atelier includes its Delivery camera and the served world bundle contains the new truck builder. See [release observation](review/graphics-v2/published-release.json).

The browser restored the existing paused checkpoint at 1:40 with one parked vehicle after replacement and displayed “Live session connected”. Genuine authenticated provider responses remain a separate pending check; this release did not make paid model calls. Rollback graphics source: `5dab074` (retain current private deployment secrets and VM settings).

Post-release browser run advanced from 1:40 to 2:05, changed station states and throughput, and responded to Pause. The public tab was left paused at 2:05.


## Production-cell detail published — 2026-09-20

Source `62073fe`, preserved Replit history in merge `43c2971`, deployed with status Live on the same 0.5 vCPU / 2 GiB VM. Local 111 tests and production build passed. The public atelier serves the new Production camera; health is OK with both providers configured and access required. Browser recovery restored 2:05 and a connected stream; running advanced to 2:15, including a five-module joining reservation and taxi-2 entering parking. [Release observation](review/graphics-v2/production-detail-release.json) and [actual 1080p modeling review](review/graphics-v2/production-detail-1080p.png) are retained. No fresh stress/soak or paid-provider certification is claimed for this small visual pass. Rollback source: `35706e1`.


## Genuine production AI acceptance — 2026-09-20

Both public-provider advisory checks passed through the native Chrome Intelligence panel using the existing private deployment access code. Astra responded in 6,767 ms (1,497 reported tokens), proposing `start` for the paused healthy run; the UI recorded that it was not applied in advisory mode. Jev returned hold in 384 ms (2,020 reported tokens), with no operational change. [Sanitized evidence](review/graphics-v2/live-ai-acceptance.json) records scope. These are real provider responses, not configured flags or fixtures, and supersede the earlier pending authenticated-production check. They do not prove autonomous improvement or long-run reliability. Server/provider code is unchanged by the visual update.


## Motion and atmosphere published — 2026-09-20

Source `1967dac`, Replit merge `be8562c`, is confirmed **Live** on the unchanged 0.5 vCPU / 2 GiB VM. Public health is OK and live AI remains access-controlled. The served atelier includes the new Humanoids camera. Browser recovery restored the paused 2:15 checkpoint; production and humanoid patrols visibly advanced, then Pause stopped the review at 2:37. See [release observation](review/graphics-v2/motion-release.json) and [public runtime capture](review/graphics-v2/motion-public.png). Rollback source: `a822715`.

117 tests across 15 files and the production build passed. Independent wheel motion, three decorative humanoid patrols, warm task lighting and cutaway refinements are described in the [graphics review](review/graphics-v2/review.md). The 1080p 32-vehicle production-renderer stress window averaged 38.6 FPS (p5 32.5; minimum 28.7), so a sustained 30 FPS floor and 60 FPS normal-scene target are not certified. No fresh two-hour soak or measured 80-percent reference-image match is claimed.

The fresh [bounded public REST/WebSocket smoke](review/graphics-v2/motion-production-smoke.json) is **BLOCKED**, not passed. Health, authenticated WebSocket connection, cross-session token rejection (401), and unauthorized Astra/Jev rejection (403) passed. Two finite one-order runs hit 70/90-second timeouts before the required completed-and-dispatched condition; the harness failed to retain terminal snapshots. Consequently this run establishes neither an end-to-end dispatch pass nor a production regression, and does not certify material balance. Preserve this failure and improve the timeout diagnostics before repeating acceptance. The older passed soak remains historical evidence, not a substitute for this release check.


## Accepted milestone and goal closure — 2026-09-20

The owner accepted the current visuals and requested closure after checking the deployed application. A fresh default balanced-production run on Replit **passed**: 75 incoming material units received, 3 vehicles completed, and 1 dispatched by simulation time 195 seconds. All seven sampled material and receiving conservation deltas were zero. 160 authenticated WebSocket frames advanced without sequence regression; a second session remained unchanged, cross-session credentials returned 401, and anonymous Astra/Jev requests returned 403. Both disposable test sessions were paused and confirmed time-stable. See [retained closeout evidence](review/graphics-v2/goal-closeout.json).

The public browser also advanced the existing factory to 16:51 with 19 dispatches and 2 parked vehicles, responded to Pause, and restored that paused checkpoint with a connected live stream after reload. The previously recorded real advisory provider responses remain valid evidence; no new paid calls were made during closeout. No runtime changes or republish were needed.

This closes the **owner-accepted virtual factory/demo milestone**. The earlier finite-one-order timeout is retained as a separate unresolved test case; the current default-flow pass does not explain or erase it. Further hero-image refinement, strict performance-floor certification, formal audio/accessibility review, durable external archival, and physical commissioning remain future work, not claims of this closure.


## Network animation jitter correction — 2026-09-20

Source `339cccb` replaces per-packet animation-time resets with a monotonic presentation clock. Incoming authoritative snapshots gently adjust playback pace; normal network arrivals no longer rewind every animated object. Pause/step and session/epoch reset remain authoritative; extrapolation is bounded on disconnection. Simulation, inventory and server timing are unchanged.

Eight focused animation-clock/wheel tests passed, including irregular/bunched packets at 1x and 10x, paused/stepped time, reset and disconnected bounds; typecheck and production build passed. A same-stream comparison against real public WebSocket arrivals recorded 58 backward steps with the former formula and zero with the corrected clock across 861 samples/78 packets over 15 seconds. Largest old reversal was 0.123 simulated seconds. [Timing evidence](review/graphics-v2/jitter-timing.json) is a presentation-clock check, not a GPU performance benchmark. The local renderer was refreshed and visually reviewed after hot reload.

Replit merge `5d2e659` was published with destination status **Live** on the existing VM. The public page serves the exact locally built corrected renderer `world-C_JaZ9Vy.js` and app `app-B9DRbT4w.js`; health is OK and AI access remains required. Browser reload recovered the 16:51 checkpoint; at 1x production advanced through 17:06 and dispatch rose from 19 to 20, with the corrected scene rendered and Pause checked. Rollback source: `438447b`.


## Server and renderer smoothness correction — 2026-09-20

Server source `385eb75` eliminates repeated full-history cloning from scheduler housekeeping, bypasses aftermath snapshots when no aftermath is pending, limits live display events to 200 without trimming export/history state, shares serialized messages per session/sequence, and avoids queueing obsolete snapshots on slow sockets. Renderer source `18c29ff` batches rigid station scenery and robot segments while preserving articulated transforms, textured labels and per-facade cutaway behavior. Anti-aliasing and material-freeze probes did not give a reliable gain, so those settings remain unchanged.

All 122 tests across 17 files passed, and the production build passed. Five aged factories (starting at 1,800 simulated seconds) passed local and deployed bounded load checks with zero material/receiving discrepancies and advancing dispatch. The deployed check sampled 3.93 ms scheduler processing with ten sessions/viewers present; this metric excludes socket serialization and is not the whole event-loop budget. Public stream p95 arrival gaps remained 263–314 ms with isolated roughly one-second gaps during this load test. The configured 45-second dwell excludes sequential setup/collection overhead, so per-stream progression includes that overhead. These checks do not substitute for a new two-hour soak.

[Initial Chrome diagnosis](review/graphics-v2/chrome-smoothness-audit.json), [temporary rendering comparison](review/graphics-v2/render-batching-probe.json), [local load](review/graphics-v2/server-stream-load.json), and [public load](review/graphics-v2/server-stream-production-load.json) preserve the measured results, including slow frames. Final post-publication Chrome measurement follows.

The intermediate published Chrome check restored real-time pace (29.75 simulated seconds over 29.936 wall seconds) and averaged 59.5 FPS, but retained 24 frames over 50 ms. Inspection then found station gripper/transfer progress still sampled from network packets. The final pass derives in-flight progress per render frame from authoritative phase bounds and the monotonic presentation clock; pause/fault/maintenance hold their reported progress. It also batches each rigid in-process component stage, retaining independent placement/visibility. All 123 tests across 17 files and production build passed. [Intermediate published results](review/graphics-v2/stream-batching-published-check.json) remain retained.

Final source `eda35f9` / Replit merge `800dff0` is confirmed Live, serving `world-DrbUL5ZN.js`. Final foreground Chrome measurement at 1512x772 CSS pixels (DPR 2), after five seconds of warm-up: **70.47 FPS average**, p95 20.7 ms, p99 25.1 ms, maximum 38.2 ms, two of 1,762 measured intervals over 33.4 ms and **zero over 50 ms**. Across 240 public stream messages, 29.875 simulated seconds elapsed over 30.032 wall seconds at 1x; p95 network gap 182.5 ms, maximum 226.3 ms, all sampled material deltas zero. The inspector, labels, articulated machinery and humanoids rendered correctly; production reached nine dispatches, and Pause stopped at 9:22. Health remained OK with AI access required. [Final evidence](review/graphics-v2/smoothness-final-chrome.json). This is a bounded same-machine check with other desktop workloads, not an all-hardware or uninterrupted-60-FPS certification. Rollback before this performance series: `ae9fe65`.
