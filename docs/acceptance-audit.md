# Acceptance gap audit

Audit date: 2026-09-19. This began as a read-only implementation and evidence review, then concrete UI gaps were addressed and independently browser-checked in the same working session. `PASS` means the cited evidence directly supports the criterion; `PARTIAL` means part of it works but a required part is absent or has no acceptance evidence; `NOT RUN` means it was not verified; `RUNNING` is an active check whose current samples are not a result.

## Verified baseline

| Area | Status | Exact evidence |
| --- | --- | --- |
| Type safety, unit/integration suite, production bundle | PASS, local scope | The final release source passed `npm run typecheck`, production build, repository audit, and **107 automated tests across 12 files**. The localhost:3010 runtime includes station pause, defect instrumentation, and adaptive-experiment routes. Vite's Babylon world-chunk warning was non-failing. This is local release evidence, not Reserved VM publication evidence. |
| Five-line material flow and vehicle journey in simulation | PASS | `tests/simulation.test.ts` asserts concurrent front/rear/battery/interior/exterior work, a five-module vehicle, deliveries, completion, dispatch, lot lineage, capacity backpressure, and repair. `docs/verification.md` records 37 simulation/archive tests and an accelerated soak. |
| Scenario coverage and faults | PASS | The shared contract has eight IDs: balanced flow, six disruptions (shortage, slow exterior, gripper, congestion, assembly outage, dispatch blockage), and empty start. `docs/verification.md` records every canonical scenario progressing after applicable recovery, and the test suite covers station, supply, congestion, assembly, dispatch, quality, and receiving-rejection paths. |
| Bounded in-memory checkpoint/replay | PASS | `tests/simulation.test.ts` and `tests/simulation-archive.test.ts` cover paused restore, retained-history caps, malformed checkpoint rejection, and deterministic replay. The bounds are explicitly documented in `docs/verification.md`. |
| Full session archive boundary | PASS | `tests/server.test.ts` covers token-authenticated NDJSON history across restart and explicit quota truncation markers. The UI has separate `Export checkpoint` and `Full history` actions. |
| 1080p world captures and on-host stress | PASS, scoped | `docs/review/` contains reviewed site, receiving, storage, robot, assembly, parking, fault, dusk, exploded, and stress captures. The final post-staged-assembly 1920×1080 synthetic 32-vehicle measurement completed: 72 samples, 107.6 FPS mean, 86.9 minimum, 102.6 fifth percentile. Evidence: `docs/review/final-stress-1080p.png` and [graphics review](graphics-review.md). This does not establish performance on other hosts, audio quality, accessibility, or physical operation. |
| Provider secret boundary and access control | PASS | `packages/providers/src/index.ts` reads provider keys only from server environment. `tests/providers.test.ts` verifies the narrow operational view and rejects out-of-policy commands or invalid usage counts; `tests/server.test.ts` verifies absent-provider behavior, public access-code gating, process-wide one-in-flight admission, stale-action invalidation, and generic errors without credential exposure. |
| Runtime response and live-stream validation | PASS, scoped | Shared Zod contracts describe snapshots, provider status, decisions, checkpoints, experiment results, command results, and WebSocket snapshot frames. The web client parses REST responses and rejects malformed, wrong-session, and stale-sequence updates before rendering. The release source passed typecheck/build/107 tests; port-3010 monitoring observed zero rejected updates in a short review. Deliberately injected malformed-frame browser behavior was not exercised. |
| Deterministic order and material-allocation policy | PASS, scoped | Source selects uncommitted production orders by priority, then creation time and numeric ID. Dedicated carts compete for configurable 1–3 shared loader slots by material priority, then fixed line order; committed carts are not preempted. Priority changes create events that name the policy. Focused tests are recorded in `docs/verification.md`; port-3010 browser review created and observed a new priority-2 order. It did not independently demonstrate a material-priority tie break. |
| Original asset provenance | PASS | `docs/assets/README.md` inventories the original concept, procedural runtime assets, Lucide, and locally served `studio.env` with CC BY 4.0 source/license; the footer links to that attribution. |

## Material gaps before calling the user experience complete

| Area | Status | Exact finding | Required resolution |
| --- | --- | --- | --- |
| Full metrics in the UI | PASS, scoped | Browser review rendered receiving/sorting, conservation, monitoring, and chart selector data; selecting Parking occupancy updated its accessible label, unit, range, latest value, and time axis. CSV/current JSON controls issued notifications, but saved file contents remain uninspected. | Inspect saved download contents if file-level evidence is needed. |
| Ten-paired-seed comparison disclosure | PASS, bounded live scope | Browser review completed current-configuration comparison with named seeds, values, standard deviations, variation, and captured provenance. A separate authenticated Jev adaptive run completed ten paired seeds and twenty genuine live decisions under the twelve-per-minute limit; Jev chose hold every time, so all sixteen candidate metrics matched baseline exactly. | Preserve the neutral result and the distinction between genuine live evidence, fixtures, and open-loop recorded-action replay. |
| Manual configuration controls | PASS, scoped | Browser evidence covers faults/repair, profile/buffer, bounded flow, continuous-order, layout, 12-bay caps, priority order creation, and station-specific pause/resume. Battery & floor held at 46.7% while other lines continued, then resumed the same module/lot. | — |
| Audio settings integration | PASS, visual UI behavior | The isolated browser run enabled the sound control, set Master to 40%, reloaded, and observed the persisted value. It also confirmed the 22%, 36%, and 58% persisted values render exactly with 1% slider increments. Audible output and spatial balance are still not independently instrumented. See `docs/browser-controls.md`. | — |
| Genealogy, active-entity inspection, and graphical replay | PARTIAL | Browser evidence includes `taxi-9` with five module rows and replay to a new paused session; the current 3010 review inspected truck/cart fields and resumed the same paused battery module/lot. Rework history was browser-observed for module-front-9: axle-alignment rejection, one repair attempt, then acceptance; evidence is in `review/rework-inspector.png` and `.txt`. Invalid/future and quota-truncated replay rejection pass API tests; browser error presentation remains unobserved. | Browser-check replay error presentation if additional UI evidence is required. |
| Live Astra/Jev operation and adaptive policy | PASS, bounded local scope | Authorized checks produced genuine Astra and Jev advisories. Astra applied a validated front-station repair in autonomous mode; a separate run retained its complete 120-simulated-second observational aftermath. Browser Astra requested a bounded ten-seed experiment without changing the live profile. Jev completed twenty genuine adaptive decisions and chose hold throughout. Sanitized evidence is linked from `docs/release-evidence.md`. | Broader provider-quality, cost, stability, and long-duration autonomous evaluation remains separate from this bounded acceptance. |
| Browser command and recovery acceptance | PARTIAL | `docs/browser-controls.md` records Start → 10× without a stale-revision error, paused graphical replay, and reload recovery to a paused saved checkpoint. A later production-local run also verified repeated reloads without new sessions and real server-restart recovery into a paused checkpoint. Archive download contents, import/export, screenshot-download persistence, and the latest WebSocket sequence rejection behavior lack browser assertions. | Add browser/component coverage or an explicit scripted browser run for server restart, export/import, history download, capture, and invalid/stale live-frame handling. |
| Accessibility baseline | PARTIAL, bounded | A production-local keyboard smoke check reached named top-bar controls with Tab, activated dusk with Space, and opened details with Enter. Source includes icon labels, focus styling, and reduced-motion CSS. This is a bounded baseline, not formal accessibility certification. | Optional broader keyboard, contrast, screen-reader, and reduced-motion review. |
| Container and Node 22 local runtime | PASS, local scope | Final Node 22 smoke and local Docker smoke passed health, static SPA, session, paired comparison, replay, and worker routes. Reserved VM publication follows local acceptance and account setup. | — |
| Documentation consistency for archive retention | PASS | During this audit, `docs/verification.md` was refreshed to describe the private NDJSON archive, retention, quota-truncation marker, and programmatic replay boundary consistently with `docs/configuration.md` and the server. | — |

## Claims that remain intentionally out of scope

Physical machine behavior, gripper forces, safety validation, actual production metrics, and physical commissioning are not simulated proof. The system and documents state this boundary correctly; do not change that status without a separate physical-validation program.

## Recommended closure order

1. The corrected clean-harness two-hour soak now passes: 121 samples through 7200.012 seconds with strict frame progress, zero conservation/stream errors, and bounded retention. Its [assessment](review/release-soak-corrected-assessment.md) records the exact counts and hashes; the failed pre-fix artifact remains historical.
2. Inspect downloaded CSV/JSON, checkpoint, and history contents if file-level evidence is needed.
3. Bounded live-provider evidence is complete; broader provider evaluation remains separate. A human audio listening review is unrecorded. Replit Reserved VM publication requires its own deployment evidence; formal accessibility certification is not a local-release gate.
